# Palindrome-Checker
A simplr palindrome checker that uses a for loop to reverse the string and compares the original and reverse srings

FLOW
usecase1:
  just a welcome message
usecase2:
  a hardcoded string that goes through reversal and matching
usecase3:
  a scanner string
